# Landing SeaScope V2 — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommandé) ou superpowers:executing-plans. Steps en cases à cocher `- [ ]`.

**Goal:** Refondre la landing publique en V2 « assistant de décision plaisance », optimisée conversion Google Play, sur 12 sections narratives.

**Architecture:** Next.js App Router (existant). Deux fondations d'abord (assets optimisés + module prix source-unique), puis primitives UI partagées, puis les sections, puis composition page + Nav/Footer + SEO, puis nettoyage bêta. Identité SeaScope conservée (tokens navy/teal, Geist, primitives existantes).

**Tech Stack:** Next.js (App Router), TypeScript, Tailwind (tokens `tailwind.config.ts`), `next/image`, `next/font` (Geist), `sharp` (assets), node `--experimental-strip-types` (tests scripts).

## Global Constraints

- Repo `seascope-landing` uniquement. Ne PAS toucher `seaScope`.
- Identité conservée : couleurs `tailwind.config.ts` (`ss-bg` #061425, `ss-teal` #5EEAD4, verdict `ss-bon`/`ss-variable`/`ss-deconseille`), police Geist, primitives `components/ui/*` réutilisées.
- Prix : SOURCE UNIQUE `lib/pricing.ts`, miroir de `seaScope/lib/paywall/offerings.ts`. Valeurs verbatim : Premium 5,99 €/`P1M`, 39,99 €/`P1Y` (badge « Économisez 44 % ») ; Premium+ 9,99 €/`P1M`, 69,99 €/`P1Y` (« Économisez 42 % »), Pass saison 24,99 €/`P4M_NON_RENEWING` (« Pass saison · 4 mois »). Aucune autre section ne code un prix en dur.
- Retirer de la page publique : `BetaForm`, `FeedbackForm`, `FeedbackLoop`. Conserver intacts `/admin`, `/redeem`, `/privacy`, routes API.
- CTA unique = « Télécharger gratuitement sur Google Play ». Bouton App Store présent mais désactivé (« Bientôt sur iOS »).
- Domaine canonique : `https://seascope-web.pennarstudio.fr` (déjà `NEXT_PUBLIC_SITE_URL`).
- FR uniquement. `prefers-reduced-motion` respecté. Un seul `<h1>` (Hero).
- Vérif projet : `pnpm typecheck` (0 erreur) + `pnpm build` (OK) ; tests scripts via `node --experimental-strip-types`.

---

### Task 1 — Module prix source unique `lib/pricing.ts` (+ test node)

**Files:**
- Create: `lib/pricing.ts`
- Create (test): `scripts/test-pricing.ts` (calque `scripts/test-beta-token.ts`)

**Interfaces:**
- Produces :
  - `type Period = 'P1M' | 'P1Y' | 'P4M_NON_RENEWING'`
  - `interface Product { id: string; role: 'monthly' | 'annual' | 'season'; period: Period; priceEUR: number }`
  - `const PRODUCTS: Record<'premiumMonthly'|'premiumAnnual'|'premiumPlusMonthly'|'premiumPlusAnnual'|'premiumPlusSeason', Product>`
  - `interface OfferingPackage { role: 'monthly'|'annual'|'season'; product: Product; highlighted: boolean; badge?: string }`
  - `const OFFERINGS: { premium: { title: string; packages: OfferingPackage[] }; premiumPlus: { title: string; packages: OfferingPackage[] } }`
  - `function monthlyEquivalentEUR(p: Product): number | null`
  - `function formatEUR(n: number): string`  // « 39,99 € »

- [ ] **Step 1 — Test qui échoue** : `scripts/test-pricing.ts`
```ts
import assert from 'node:assert/strict'
import { PRODUCTS, OFFERINGS, monthlyEquivalentEUR, formatEUR } from '../lib/pricing.ts'

// 5 produits, valeurs miroir de seaScope/lib/paywall/offerings.ts
assert.equal(PRODUCTS.premiumMonthly.priceEUR, 5.99)
assert.equal(PRODUCTS.premiumAnnual.priceEUR, 39.99)
assert.equal(PRODUCTS.premiumPlusMonthly.priceEUR, 9.99)
assert.equal(PRODUCTS.premiumPlusAnnual.priceEUR, 69.99)
assert.equal(PRODUCTS.premiumPlusSeason.priceEUR, 24.99)
assert.equal(PRODUCTS.premiumPlusSeason.period, 'P4M_NON_RENEWING')

// offerings : annuel mis en avant + badges
const premAnnual = OFFERINGS.premium.packages.find(p => p.role === 'annual')!
assert.equal(premAnnual.highlighted, true)
assert.equal(premAnnual.badge, 'Économisez 44 %')
assert.equal(OFFERINGS.premiumPlus.packages.find(p => p.role === 'annual')!.badge, 'Économisez 42 %')
assert.equal(OFFERINGS.premiumPlus.packages.find(p => p.role === 'season')!.badge, 'Pass saison · 4 mois')

// cohérence : annuel < 12 × mensuel (l'économie est réelle)
assert.ok(PRODUCTS.premiumAnnual.priceEUR < 12 * PRODUCTS.premiumMonthly.priceEUR)
assert.ok(PRODUCTS.premiumPlusAnnual.priceEUR < 12 * PRODUCTS.premiumPlusMonthly.priceEUR)

// helpers
assert.equal(monthlyEquivalentEUR(PRODUCTS.premiumAnnual), 3.33)
assert.equal(formatEUR(39.99), '39,99 €')
console.log('Tous les tests pricing passent.')
```
- [ ] **Step 2 — Lancer, voir échouer** : `node --experimental-strip-types scripts/test-pricing.ts` → `ERR_MODULE_NOT_FOUND` (lib/pricing.ts absent).
- [ ] **Step 3 — Implémenter** `lib/pricing.ts` :
```ts
// SOURCE UNIQUE des tarifs de la landing.
// Miroir de seaScope/lib/paywall/offerings.ts — recopier ici si les prix changent.
export type Period = 'P1M' | 'P1Y' | 'P4M_NON_RENEWING'
export interface Product { id: string; role: 'monthly' | 'annual' | 'season'; period: Period; priceEUR: number }

export const PRODUCTS = {
  premiumMonthly:     { id: 'seascope_premium_monthly',      role: 'monthly', period: 'P1M', priceEUR: 5.99 },
  premiumAnnual:      { id: 'seascope_premium_annual',       role: 'annual',  period: 'P1Y', priceEUR: 39.99 },
  premiumPlusMonthly: { id: 'seascope_premium_plus_monthly', role: 'monthly', period: 'P1M', priceEUR: 9.99 },
  premiumPlusAnnual:  { id: 'seascope_premium_plus_annual',  role: 'annual',  period: 'P1Y', priceEUR: 69.99 },
  premiumPlusSeason:  { id: 'seascope_premium_plus_season',  role: 'season',  period: 'P4M_NON_RENEWING', priceEUR: 24.99 },
} as const satisfies Record<string, Product>

export interface OfferingPackage { role: 'monthly' | 'annual' | 'season'; product: Product; highlighted: boolean; badge?: string }
export const OFFERINGS = {
  premium: {
    title: 'Premium',
    packages: [
      { role: 'annual',  product: PRODUCTS.premiumAnnual,  highlighted: true,  badge: 'Économisez 44 %' },
      { role: 'monthly', product: PRODUCTS.premiumMonthly, highlighted: false },
    ],
  },
  premiumPlus: {
    title: 'Premium+',
    packages: [
      { role: 'annual',  product: PRODUCTS.premiumPlusAnnual,  highlighted: true,  badge: 'Économisez 42 %' },
      { role: 'season',  product: PRODUCTS.premiumPlusSeason,  highlighted: false, badge: 'Pass saison · 4 mois' },
      { role: 'monthly', product: PRODUCTS.premiumPlusMonthly, highlighted: false },
    ],
  },
} as const

export function monthlyEquivalentEUR(p: Product): number | null {
  if (p.period === 'P1Y') return Math.round((p.priceEUR / 12) * 100) / 100
  if (p.period === 'P1M') return p.priceEUR
  return null
}
export function formatEUR(n: number): string {
  return `${n.toFixed(2).replace('.', ',')} €`
}
```
- [ ] **Step 4 — Relancer** : `node --experimental-strip-types scripts/test-pricing.ts` → « Tous les tests pricing passent. »
- [ ] **Step 5 — typecheck** : `pnpm typecheck` → 0 erreur.
- [ ] **Step 6 — Commit** : `git add lib/pricing.ts scripts/test-pricing.ts && git commit -m "feat(pricing): source unique des tarifs (miroir offerings.ts) + test"`

---

### Task 2 — Pipeline assets `scripts/prepare-screens.ts` → `public/screens/*.webp`

**Files:**
- Create: `scripts/prepare-screens.ts`
- Produces (générés): `public/screens/{dashboard-decision,forecast-today,forecast-7days,weather,map,navigation,guardian,settings-sources,paywall-tarifs,paywall-comparatif,paywall-avantages}.webp`

**Interfaces:**
- Consumes : les 11 JPG sources dans `C:/Users/nvainer/Downloads/Seascope_files/`.
- Produces : 11 webp optimisés (barre d'état OS rognée), largeur max 900 px.

- [ ] **Step 1 — Implémenter le script** (sharp est déjà dans `node_modules` ; sinon `pnpm add -D sharp`) :
```ts
import sharp from 'sharp'
import { mkdirSync } from 'node:fs'

const SRC = 'C:/Users/nvainer/Downloads/Seascope_files'
const OUT = 'public/screens'
mkdirSync(OUT, { recursive: true })

// mapping source → nom sémantique. topCropPx = pixels de barre d'état OS à rogner.
const MAP: Array<{ src: string; out: string; topCropPx: number }> = [
  { src: '22601.jpg', out: 'dashboard-decision', topCropPx: 72 },
  { src: '22605.jpg', out: 'forecast-today',      topCropPx: 72 },
  { src: '22606.jpg', out: 'forecast-7days',      topCropPx: 72 },
  { src: '22608.jpg', out: 'weather',             topCropPx: 72 },
  { src: '22614.jpg', out: 'map',                 topCropPx: 72 },
  { src: '22615.jpg', out: 'navigation',          topCropPx: 72 },
  { src: '22617.jpg', out: 'settings-sources',    topCropPx: 72 },
  { src: '22619.jpg', out: 'paywall-tarifs',      topCropPx: 72 },
  { src: '22621.jpg', out: 'paywall-comparatif',  topCropPx: 72 },
  { src: '22623.jpg', out: 'paywall-avantages',   topCropPx: 72 },
  { src: '22603.jpg', out: 'dashboard-full',      topCropPx: 0  }, // déjà sans barre visible
]

for (const m of MAP) {
  const img = sharp(`${SRC}/${m.src}`)
  const meta = await img.metadata()
  const h = (meta.height ?? 0) - m.topCropPx
  await img
    .extract({ left: 0, top: m.topCropPx, width: meta.width!, height: h })
    .resize({ width: 900, withoutEnlargement: true })
    .webp({ quality: 82 })
    .toFile(`${OUT}/${m.out}.webp`)
  console.log(`✓ ${m.out}.webp`)
}
console.log('Assets prêts.')
```
- [ ] **Step 2 — Lancer** : `node --experimental-strip-types scripts/prepare-screens.ts` → 11 lignes `✓ …webp` + « Assets prêts. »
- [ ] **Step 3 — Vérifier les sorties** : `ls public/screens/*.webp | wc -l` → 11. Ouvrir `dashboard-decision.webp` pour confirmer que la barre d'état OS est rognée (ajuster `topCropPx` par image si besoin, re-lancer).
- [ ] **Step 4 — Commit** : `git add scripts/prepare-screens.ts public/screens && git commit -m "chore(assets): captures app optimisées (rognage barre OS + webp)"`

---

### Task 3 — Primitives UI partagées

**Files:**
- Create: `components/ui/PhoneMock.tsx`, `components/ui/StoreButton.tsx`, `components/ui/PricingCard.tsx`, `components/ui/ComparisonTable.tsx`, `components/ui/FeatureRow.tsx`, `components/ui/Accordion.tsx`
- Consulter (patterns existants): `components/ui/Button.tsx`, `DeviceFrame.tsx`, `SectionHeader.tsx`, `Reveal.tsx`, `Pill.tsx`

**Interfaces (Produces) :**
- `PhoneMock({ src, alt, priority? }: { src: string; alt: string; priority?: boolean })` — cadre téléphone (réutilise/adapte `DeviceFrame`) + `next/image` (dimensions explicites, `loading` lazy sauf `priority`).
- `StoreButton({ store, href?, disabled? }: { store: 'googlePlay' | 'appStore'; href?: string; disabled?: boolean })` — App Store → `disabled` + libellé « Bientôt sur iOS ».
- `PricingCard({ offering }: { offering: typeof OFFERINGS.premium })` — lit `lib/pricing.ts` (formatEUR, monthlyEquivalentEUR, badges).
- `ComparisonTable()` — tableau Gratuit/Premium/Premium+ (données en constante locale au composant, cf. capture 22621 ; prix éventuels via `lib/pricing.ts`).
- `FeatureRow({ title, children, image, reverse? })` — bloc alternant texte/`PhoneMock`.
- `Accordion({ items }: { items: { q: string; a: React.ReactNode }[] })` — client component, `prefers-reduced-motion` respecté.

- [ ] **Step 1 — Implémenter les 6 primitives** en suivant les tokens Tailwind existants (`bg-ss-surface`, `text-ss-fg`, `rounded-ss-lg`, `ss-teal`…) et les patterns de `components/ui/*`. `StoreButton` googlePlay = CTA teal ; appStore disabled = grisé.
- [ ] **Step 2 — typecheck** : `pnpm typecheck` → 0 erreur.
- [ ] **Step 3 — Commit** : `git add components/ui && git commit -m "feat(ui): primitives landing V2 (PhoneMock, StoreButton, PricingCard, ComparisonTable, FeatureRow, Accordion)"`

---

### Task 4 — Sections Hero + Problème

**Files:**
- Create/rewrite: `components/sections/Hero.tsx`, `components/sections/Problem.tsx`

**Interfaces:** Consomme `PhoneMock`, `StoreButton`, `SectionHeader`, `Reveal`. Produces `<Hero />`, `<Problem />`.

- [ ] **Step 1 — `Hero.tsx`** : `h1` « Puis-je sortir aujourd'hui ? » (ou variante forte), sous-titre court (« SeaScope analyse météo, vent, vagues, marées, courants et sécurité pour vous donner un verdict clair. »), `StoreButton googlePlay`, `PhoneMock src="/screens/dashboard-decision.webp" priority`. Un seul `h1` de la page.
- [ ] **Step 2 — `Problem.tsx`** : « Aujourd'hui, décider = jongler entre 5 applis » → 3-4 puces (applis météo, cartes, sites marées, sources) → « SeaScope centralise et tranche. » (pas de capture).
- [ ] **Step 3 — Vérif** : `pnpm build` OK ; revue visuelle `pnpm dev` (Hero au-dessus de la ligne de flottaison, responsive).
- [ ] **Step 4 — Commit** : `git add components/sections/Hero.tsx components/sections/Problem.tsx && git commit -m "feat(landing): sections Hero + Problème"`

---

### Task 5 — Sections Décider + Préparer

**Files:**
- Create/rewrite: `components/sections/Decide.tsx`, `components/sections/Prepare.tsx`

**Interfaces:** Consomme `FeatureRow`, `PhoneMock`, `SectionHeader`, `Reveal`.

- [ ] **Step 1 — `Decide.tsx`** : titre « Décider en quelques secondes » ; points score / verdict / créneaux idéaux / retour conseillé / simulateur ; `FeatureRow` avec `PhoneMock src="/screens/forecast-today.webp"` (+ `dashboard-decision.webp` en second visuel).
- [ ] **Step 2 — `Prepare.tsx`** : titre « Préparer ses sorties » ; Aujourd'hui / 7 jours / météo détaillée / planning ; `FeatureRow` alternés avec `forecast-7days.webp` et `weather.webp`.
- [ ] **Step 3 — Vérif** : `pnpm build` OK ; revue visuelle.
- [ ] **Step 4 — Commit** : `git add components/sections/Decide.tsx components/sections/Prepare.tsx && git commit -m "feat(landing): sections Décider + Préparer"`

---

### Task 6 — Sections Explorer + Naviguer

**Files:**
- Create/rewrite: `components/sections/Explore.tsx`, `components/sections/Navigate.tsx`

**Interfaces:** Consomme `PhoneMock`/image large, `SectionHeader`, `Pill`, `Reveal`.

- [ ] **Step 1 — `Explore.tsx`** (très visuel) : titre « Explorer » ; `map.webp` en grand format (pleine largeur ou large `PhoneMock`) ; puces couches : spots, balises, vent, pluie, courants, bathymétrie, POI (utiliser `Pill`/`Chip`).
- [ ] **Step 2 — `Navigate.tsx`** : titre « Naviguer » ; IGN, AIS, Journal, Mouillage ; `PhoneMock src="/screens/navigation.webp"`.
- [ ] **Step 3 — Vérif** : `pnpm build` OK ; revue visuelle (section Explore impactante).
- [ ] **Step 4 — Commit** : `git add components/sections/Explore.tsx components/sections/Navigate.tsx && git commit -m "feat(landing): sections Explorer + Naviguer"`

---

### Task 7 — Section Sécurité / Guardian (+ bandeau sources)

**Files:**
- Create/rewrite: `components/sections/Security.tsx`

**Interfaces:** Consomme `PhoneMock`, `SectionHeader`, `Callout`, `Reveal`.

- [ ] **Step 1 — `Security.tsx`** : Guardian au centre (argument sécurité majeur) — Guardian Watch, verdict mouillage, alertes, surveillance d'ancre ; visuels `dashboard-decision.webp` (bandeau « Mouillage autorisé ») + `settings-sources.webp` (Guardian) ; **bandeau confiance** « Données Météo-France AROME · Open-Meteo · SHOM » (depuis `settings-sources.webp`).
- [ ] **Step 2 — Vérif** : `pnpm build` OK ; revue visuelle.
- [ ] **Step 3 — Commit** : `git add components/sections/Security.tsx && git commit -m "feat(landing): section Sécurité / Guardian + bandeau sources"`

---

### Task 8 — Sections Comparatif + Tarifs

**Files:**
- Create/rewrite: `components/sections/Comparison.tsx`, `components/sections/Pricing.tsx`

**Interfaces:** Consomme `ComparisonTable`, `PricingCard`, `lib/pricing.ts`, `StoreButton`.

- [ ] **Step 1 — `Comparison.tsx`** : titre « Comparatif » ; `ComparisonTable` (hiérarchie de la capture 22621 conservée : lignes Sécurité en premier, tags BIENTÔT, Gratuit toujours inclus). Aucun prix en dur (via `lib/pricing.ts` si cité).
- [ ] **Step 2 — `Pricing.tsx`** : titre « Tarifs » ; `PricingCard` pour `OFFERINGS.premium` et `OFFERINGS.premiumPlus` ; mention « Gratuit, pour toujours » ; note « sans engagement, résiliable à tout moment ». Prix exclusivement depuis `lib/pricing.ts`.
- [ ] **Step 3 — Vérif** : `pnpm build` OK ; revue visuelle ; vérifier que les prix affichés = 39,99/5,99/69,99/24,99/9,99.
- [ ] **Step 4 — Commit** : `git add components/sections/Comparison.tsx components/sections/Pricing.tsx && git commit -m "feat(landing): sections Comparatif + Tarifs (source unique prix)"`

---

### Task 9 — Sections Histoire + FAQ + CTA final

**Files:**
- Create/rewrite: `components/sections/Story.tsx`, `components/sections/FAQ.tsx`, `components/sections/FinalCta.tsx`
- Create: `lib/faq.ts` (Q/R partagées section FAQ + JSON-LD)

**Interfaces:** Consomme `Accordion`, `StoreButton`, `SectionHeader`. `lib/faq.ts` produces `FAQ_ITEMS: { q: string; a: string }[]`.

- [ ] **Step 1 — `lib/faq.ts`** : les 6 Q/R du brief (différence vs apps météo ; gratuit ? ; pourquoi Premium ? ; d'où viennent les données ? ; iPhone prévu ? ; fiabilité). Réponses courtes, ton humain.
- [ ] **Step 2 — `Story.tsx`** : « Pourquoi SeaScope existe » — récit fondateur (plaisancier récent, jonglait entre apps, a créé SeaScope pour transformer des données marines complexes en décisions simples). Ton humain, authentique.
- [ ] **Step 3 — `FAQ.tsx`** : `Accordion items={FAQ_ITEMS}`.
- [ ] **Step 4 — `FinalCta.tsx`** : titre fort + `StoreButton googlePlay` + `StoreButton appStore disabled`.
- [ ] **Step 5 — Vérif** : `pnpm build` OK ; revue visuelle.
- [ ] **Step 6 — Commit** : `git add components/sections/Story.tsx components/sections/FAQ.tsx components/sections/FinalCta.tsx lib/faq.ts && git commit -m "feat(landing): sections Histoire + FAQ + CTA final"`

---

### Task 10 — Composition page + Nav + Footer + retrait bêta

**Files:**
- Modify: `app/page.tsx`, `components/layout/Nav.tsx`, `components/layout/Footer.tsx`
- Delete: `components/sections/BetaForm.tsx`, `components/sections/FeedbackForm.tsx`, `components/sections/FeedbackLoop.tsx`
- Vérifier/adapter: `components/sections/HowItWorks.tsx`, `Personalization.tsx`, `Trust.tsx` (anciennes sections bêta non réutilisées → supprimer si plus référencées)

**Interfaces:** Consomme toutes les sections des Tasks 4-9.

- [ ] **Step 1 — `app/page.tsx`** : composer `Nav` + `main` { Hero, Problem, Decide, Prepare, Explore, Navigate, Security, Comparison, Pricing, Story, FAQ, FinalCta } + `Footer`. Retirer BetaForm/FeedbackForm/FeedbackLoop.
- [ ] **Step 2 — `Nav.tsx`** : liens ancres vers les nouvelles sections + `StoreButton googlePlay` compact. `Footer.tsx` : mentions + lien `/privacy` (pas de capture email).
- [ ] **Step 3 — Supprimer** les fichiers de sections bêta désormais non importés (`BetaForm`, `FeedbackForm`, `FeedbackLoop`, et anciennes `HowItWorks`/`Personalization`/`Trust` si plus utilisées). Vérifier via `grep -rl` qu'aucune n'est importée ailleurs.
- [ ] **Step 4 — Vérif** : `pnpm typecheck` 0 erreur + `pnpm build` OK ; `pnpm dev` → scroll complet des 12 sections, responsive mobile/desktop.
- [ ] **Step 5 — Commit** : `git add -A && git commit -m "feat(landing): composition page V2 + Nav/Footer, retrait sections bêta"`

---

### Task 11 — SEO + metadata + JSON-LD

**Files:**
- Modify: `app/layout.tsx` (metadata), `app/page.tsx` (JSON-LD)
- Consulter: `lib/faq.ts` (pour FAQPage)

**Interfaces:** Consomme `FAQ_ITEMS`.

- [ ] **Step 1 — `app/layout.tsx`** : `metadata` (title « SeaScope — l'assistant de décision des plaisanciers », description autour de « météo marine, prévisions, sécurité, mouillage, planifiez vos sorties »), `openGraph` + `twitter` (image `/screens/dashboard-decision.webp` ou une OG dédiée), `metadataBase` = NEXT_PUBLIC_SITE_URL.
- [ ] **Step 2 — JSON-LD** dans `app/page.tsx` : `<script type="application/ld+json">` avec `SoftwareApplication` (name, applicationCategory `TravelApplication`/`UtilitiesApplication`, operatingSystem `ANDROID`, offers `Offer` price 0 EUR) + `FAQPage` généré depuis `FAQ_ITEMS`.
- [ ] **Step 3 — Vérif** : `pnpm build` OK ; vérifier le HTML rendu (`pnpm dev` → view-source) contient le JSON-LD valide + le `h1` unique.
- [ ] **Step 4 — Commit** : `git add app/layout.tsx app/page.tsx && git commit -m "feat(landing): SEO metadata + JSON-LD SoftwareApplication/FAQPage"`

---

### Task 12 — Vérification finale + revue visuelle responsive

- [ ] **Step 1** : `pnpm typecheck` → 0 erreur.
- [ ] **Step 2** : `pnpm build` → succès (pas de warning bloquant).
- [ ] **Step 3** : `node --experimental-strip-types scripts/test-pricing.ts` → passe.
- [ ] **Step 4** : `pnpm dev` → revue visuelle mobile (375px) + desktop (1280px) des 12 sections ; vérifier CTA Google Play présents (Hero, Nav, FinalCta), prix corrects, images nettes (barre OS rognée), `prefers-reduced-motion` neutralise les animations.
- [ ] **Step 5** : confirmer `/admin`, `/redeem`, `/privacy` toujours fonctionnels (non touchés).

## Self-Review (auteur)
- Couverture spec §3 (12 sections) → Tasks 4-9 + composition Task 10. §4 structure → Tasks 3,10. §5 pricing → Task 1. §6 assets → Task 2. §7 SEO/a11y → Task 11 (+ reduced-motion dans primitives Task 3 / composition). §2 retrait bêta / conserver admin → Task 10 + Step 5 Task 12. §8 tests → Tasks 1,12.
- Placeholders : aucun TBD ; textes marketing définitifs à affiner (noté §9 spec) mais chaque section a un contenu de départ concret.
- Cohérence types : `lib/pricing.ts` (PRODUCTS/OFFERINGS/monthlyEquivalentEUR/formatEUR) défini Task 1, consommé Tasks 3/8 avec les mêmes noms ; `PhoneMock`/`StoreButton`/`PricingCard`/`ComparisonTable`/`FeatureRow`/`Accordion` définis Task 3, consommés Tasks 4-9 avec les signatures déclarées.
